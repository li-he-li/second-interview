---
tags: [tests, m1, m2, m3, m4, m5, m6, m7, m8, m9, m10, m11, final]
date: 2026-06-24
summary: M1-M11 + 最终审查测试报告：117 passed 1 skipped，CLI 功能测试通过
status: done
---

# 测试报告

## 环境

- OS：Windows 11；Python 3.12.4；虚拟环境 `.venv`
- 依赖：pytest 9.1.1、pydantic 2.13.4、tiktoken 0.13.0、openai 2.43.0
- 配置目录：`config/`（默认）；真实 LLM：DeepSeek `deepseek-v4-pro`

## 稳定测试命令

```bash
.venv/Scripts/python.exe -m pytest tests/
```

- 临时目录由 `conftest.py` 动态生成项目内唯一子目录 `.temp/pytest-<时间戳>-<pid>`。
- pytest cache 已禁用（`addopts = "-p no:cacheprovider"`），0 warning。
- 真实 LLM 测试默认 skip，需 `REAL_LLM=1` 且有效 `DEEPSEEK_API_KEY` 才运行。

## 结果摘要（2026-06-24）

| 模块 | 测试文件 | 用例数 | 结果 |
| --- | --- | --- | --- |
| M1 基础设施 | `tests/test_infra.py` | 11 | passed |
| M2 知识库 | `tests/test_knowledge.py` | 9 | passed |
| M3 Mock 工具 | `tests/test_tools.py` | 11 | passed |
| M4 意图识别 | `tests/test_intent.py` | 9 | passed |
| M5 安全策略 | `tests/test_safety.py` | 16 | passed |
| M6 人工审批 | `tests/test_approval.py` | 7 | passed |
| M7 工作记忆 | `tests/test_memory.py` | 10 | passed |
| M8 ESC 打断 | `tests/test_interrupt.py` | 6 | passed |
| M9 LLM 层 | `tests/test_llm.py` | 17 + 1 skip | passed |
| M10 Runner+CLI | `tests/test_runner.py` | 13 | passed |
| M11 失败场景 | `tests/test_failures.py` | 8 | passed |
| **合计** |  | **117 + 1 skip** | **117 passed, 1 skipped** |

### 关键验证点

- **M1-M8**：见前述（模型校验/检索/工具/意图/安全 L0-L2/审批/记忆预算裁剪/ESC 令牌）。
- **M9**：`enhance_prompt` 提取 entities/risk_hints 且**不改危险词与越界参数**；MockLLM 正常草稿 + invalid_json/timeout/error 注入；RealLLM 缺 key 报 `no_api_key`；ESC token 接入 mock 与流式。
- **真实调用验证**（`.temp/verify_real_llm.py`）：`deepseek-v4-pro` 连通，E42→qa/L0、最大速度越界→unsafe_action/L2/need_human_approval=true，返回合法 JSON。
- **最终审查补测**：runner 主流程改为通过 `search_knowledge` 工具检索并记录 tool_call；CLI real 模式传递 `DEEPSEEK_API_KEY` 与 model；工具检索超时升级 L2。
- **CLI 功能测试**：题目核心样例 E42 / 状态查询 / L1 dry-run / L2 危险拦截 / 未收录型号 / 无关输入 / 空输入均返回合法 JSON。
- **交互式 CLI**：`--session` 支持工作台式横幅、`/help`、`/status`、`/clear`、`/trace`、`/exit`；交互窗口不直接渲染完整 JSON，单轮命令仍保持纯 JSON 输出。

## 运行证据

- `runs/` 含 5 条精选提交证据（由 `.temp/generate_runs.py` 生成，mock 模式）：
  1. 设备报错 E42 → `qa` / L0 / 3 来源
  2. 机械臂抓取状态查询 → `status_check` / L0
  3. 移动到 x=100,y=50,z=20 并抓取 → `device_action` / L1 / `dry_run_executed`
  4. 最大速度移动到 x=9999 → `unsafe_action` / L2 / `blocked`
  5. 无资料查询 → `unknown` / L2 / 0 来源（不编造）
- 真实 LLM 连通证据：`.temp/verify_real_llm.py`（`deepseek-v4-pro`，E42→L0、最大速度越界→L2）。

## [21:16] 审查修复 #6 回归测试

- 用户要求：验证工具输入安全复核修复。
- 测试命令：`.venv/Scripts/python.exe -m pytest tests/`。
- 测试结果：124 passed, 1 skipped。
- 新增覆盖：LLM 在状态查询中提出越界 execute_device_command 时，审批等级必须升 L2，最终响应保持 L2；execute_device_command 工具异常记录为 failed tool_call。


## [22:30] 知识库上下文窗口回归测试

- 用户要求：验证知识库召回携带简短前后文。
- 测试命令：`.venv/Scripts/python.exe -m pytest tests/`。
- 测试结果：130 passed, 1 skipped。
- 新增覆盖：`Match` 返回 `title/context_before/context_after`；`search_knowledge` 工具输出上下文字段；上下文长度受限且不替代真实 source_id。

