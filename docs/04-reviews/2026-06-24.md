---
tags: [device-safety-agent, review, harness-engineering, m1]
date: 2026-06-24
summary: 计划门控校验 + 各 milestone 人工审查结论累积
status: in_progress
---

## [15:20] Harness Engineering 计划校验

- 用户要求：使用 `harness-engineering` 对计划做最后校验，确认覆盖是否全面，以及是否有必要遵循该 skill。
- 审查结论：建议裁剪遵循。7 Hard Gates、TDD、结构化日志、配置外部化和交付清单适用于本 CLI 面试项目；UI 标准、完整 CI/长期遥测/每阶段 commit 可作为非强制项。
- 发现问题：`plan/initial_solution.md` 中 CLI 环境变量示例残留旧工作记忆阈值，与 `config/memory_config.json` 不一致。
- 修复内容：删除旧 `MEMORY_MAX_TOKENS=1200`、`MEMORY_KEEP_LAST_TURNS=4`，改为 `CONFIG_DIR=config`；补充 `config.py` 负责加载 memory 配置。
- 改动时间：2026-06-24。

## 审查方式说明（适用于后续各 milestone）

`CLAUDE.md` 要求每个功能完成后自动触发 enterprise-code-gatekeeper。当前环境该自动审查工具不可用，按兜底规则执行等价人工审查清单，覆盖：安全/权限边界、数据一致性、错误处理、测试覆盖、范围控制、硬编码密钥/路径。

## [M1] 审查结论

- **P0**：无。
- **P1**：无。
- **安全/权限**：DeepSeek 密钥仅存 `.env`（已 gitignore）；源码/配置/测试/文档经 grep 扫描无硬编码 key；`.env.example` 不含真实值。
- **错误处理**：config 加载缺失/非法均回退保守默认并记录 `config_warning`；`trace.save` 自建目录。
- **测试覆盖**：11 条，覆盖模型字段校验、confidence 越界拒绝、配置正常/缺失/非法回退、trace 落盘、日志去重。
- **范围**：仅基础设施，无无关重构。
- **结论**：通过，可 commit。

## [M2] 审查结论

- **P0/P1**：无。
- **安全**：检索不到时返回空、绝不编造来源；`filter_sources` 过滤 LLM 编造的 source_id；无硬编码密钥。
- **错误处理**：知识库目录缺失/空文件/重复 source_id 记录 warning，不崩溃。
- **测试覆盖**：8 条，覆盖加载、source_id 格式、错误码/危险词/抓取/无关查询/编造过滤/全角转换。
- **范围**：仅知识库，无无关重构。
- **结论**：通过，可 commit。

## [M3] 审查结论

- **P0/P1**：无。
- **安全**：`execute_device_command` 默认 dry-run，`dry_run=False` 被拒（无真实执行入口）；工具白名单限制可调用工具。
- **错误处理**：`simulate=timeout/error` 可注入异常供 runner 捕获；command 类型校验。
- **测试覆盖**：11 条，覆盖三工具正常/空结果/注入异常/真实执行拒绝/状态 override。
- **结论**：通过，可 commit。

## [M4] 审查结论

- **P0/P1**：无。
- **安全**：危险词/risk_hints 强制 unsafe_action；多意图取最高风险。
- **测试覆盖**：9 条，含样例 1-4 意图边界（能力询问 vs 动作命令）。
- **结论**：通过，可 commit。

## [M5] 审查结论

- **P0/P1**：无。
- **安全**：L2 触发条件完整（unsafe_action/越界/危险词/设备拦截/unknown）；配置缺失保守默认（±1000/拒最大速度/50N/全审批）；L2 `need_human_approval` 恒 True。
- **错误处理**：空 cfg 不崩溃，回退默认。
- **测试覆盖**：15 条，覆盖参数解析/越界/力度/设备拦截/各意图定级/配置缺失。
- **结论**：通过，可 commit。

## [审查修复 #2] P0 + P1（用户反馈）

- **P0（安全治理，已清零）**：`safety._need_approval` 对 L2 不再读 `l2_requires_approval`，硬编码 `return True`；新增回归测试 `test_l2_approval_cannot_be_disabled_by_config`（配置 false 时 L2 仍 `need_human_approval=True`）。
- **P1（测试稳定性，已修复）**：移除 pyproject 固定 `--basetemp=.temp/pytest`；新增 `conftest.py` 由 `pytest_configure` 动态生成项目内唯一子目录 `.temp/pytest-<时间戳>-<pid>`，规避系统 temp 权限与固定目录复跑清理冲突；连跑两次均 55 passed。
- **结论**：P0 已清零、P1 已修复，可继续 M6。

## [M6] 审查结论

- **P0/P1**：无。
- **安全**：allyes 不降级、不改 L2→L1；非交互默认拒绝；审批状态不持久化、不写文件。
- **测试覆盖**：7 条，含 yes 单次不缓存 / no 拒绝 / allyes 全局放行 / 无 responder 默认拒绝 / 非法输入按 no 处理。
- **结论**：通过，可 commit。

## [M7] 审查结论

- **P0/P1**：无。
- **安全**：pinned safety 压缩/裁剪均不丢；记忆不作为知识库来源、不写入 sources；token 计数 tiktoken 不可用时降级估算。
- **测试覆盖**：8 条，覆盖计数/压缩触发/保留轮次/safety 不裁/硬裁剪/配置默认/上下文结构。
- **结论**：通过，可 commit。

## [M8] 审查结论

- **P0/P1**：无。
- **安全**：被打断抛 `CancelledError` 由 runner 捕获后输出合法 cancelled JSON（不崩溃）；监听线程为 daemon，不阻塞退出。
- **测试覆盖**：6 条，覆盖 cancel/check/reset/重复检查/监听器构造/runner 风格关键点检查。
- **结论**：通过，可 commit。

## [审查修复 #3] P1 memory + P1 interrupt + P2 cache（用户反馈）

- **P1（memory，已修复）**：`context_for_llm` 按 `max_memory_tokens` 裁 recent_turns、`_trim_summary` 按 `summary_max_tokens` 裁普通叙述保安全事实；2 条回归测试通过。
- **P1（interrupt，转 M9/M10 必做）**：取消令牌已具备单元覆盖，但"LLM 流式输出检查 token、工具调用、runner 输出 cancelled JSON"需在 M9/M10 真实调用链接入并加集成测试。
- **P2（cache，已修复）**：禁用 `cacheprovider`，0 warning。
- **结论**：P1/P2 已处理（interrupt 接入延后 M9/M10），可进 M9。

## [M9] 审查结论

- **P0**：无。
- **安全**：LLM 无执行权、无安全决策权；sources 只能来自 search_knowledge（runner 过滤）；`enhance_prompt` 不改危险词/越界参数、不补造来源；缺 key 明确报错不伪装成功；ESC token 接入流式循环。
- **错误处理**：非法 JSON 重试 1 次→`model_invalid_json`；超时重试→`model_timeout`；异常→`model_error`；均返回合法 `LLMResult` 供 runner fallback。
- **测试覆盖**：11 条（enhance 提取/危险边界/错误码/mock 正常/3 种注入/unsafe 草稿/缺 key/make_llm/cancel_token）+ 1 真实调用 skip。
- **真实调用**：`deepseek-v4-pro` 连通，安全判断正确（E42→L0、最大速度越界→L2）。
- **结论**：通过，可 commit。

## [审查修复 #4] M9 反馈（P1×3 + P2×2，用户反馈）

- **P1（schema 校验）**：新增 `validate_draft` 用 AgentResponse 校验枚举/范围；4 条测试（合法/非法枚举/越界 confidence/非 dict）；最终兜底由 M10 runner 接入。
- **P1（流式 cancel）**：fake streaming client 单测证明 RealLLM chunk 循环里 ESC 真能打断。
- **P1（prompt injection）**：SYSTEM_PROMPT 补不可信数据规则。
- **P2（client_init 区分）**：key 存在但初始化失败报 `model_error:client_init`，不再误报 `no_api_key`。
- **结论**：P1/P2 已处理（schema 兜底接入延后 M10），可进 M10。
