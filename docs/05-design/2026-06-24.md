---
tags: [device-safety-agent, design, plan]
date: 2026-06-24
summary: 保存制造业设备安全操作 Agent 初版方案
status: done
---

## [13:50] 初版方案落档

- 用户要求：阅读 `二面题目-device_safety_agent.md`，在 `plan/` 文件夹中保存初版方案，后续共同细化。
- 改动内容：新增 `plan/initial_solution.md`，记录技术选型、模块结构、数据流、安全策略、mock 工具、异常处理、测试样例和后续待细化问题。
- 改动时间：2026-06-24。

## [14:10] 初版方案 v2 细化

- 用户要求：确定 CLI-only，加入真实/Mock LLM 两种启动方式、L1/L2 人工审批 yes/no/allyes、知识库搜索和补充策略、ESC 强制打断、token 限制的短期记忆，并复核是否覆盖二面题目，同时评估是否引入 LangGraph。
- 改动内容：重写 `plan/initial_solution.md` 为 v2，新增覆盖性审查表；结论为首版不强依赖 LangGraph，基础流程用显式 Python pipeline 完成，LangGraph 作为基础功能通过后的可选增强。
- 改动时间：2026-06-24。

## [14:20] 真实 LLM 首选模型确认

- 用户要求：真实 LLM 首选 DeepSeek V4 Pro。
- 改动内容：更新 `plan/initial_solution.md`，将真实 LLM 默认 provider 设为 `deepseek`，默认模型设为 `deepseek-v4-pro`，配置项改为 `DEEPSEEK_API_KEY`、`DEEPSEEK_BASE_URL`、`DEEPSEEK_MODEL`。
- 改动时间：2026-06-24。

## [14:30] 结构化输出和工具注入策略补充

- 用户要求：确认结构化 JSON 如何保证，以及 LLM 如何知道工具使用说明、是否写入 system prompt。
- 改动内容：在 `plan/initial_solution.md` 新增“结构化输出和工具注入策略”章节，明确 system prompt、每轮 `available_tools/tool_results` 注入、代码层白名单和审批链路三层防护，并补充相关测试样例。
- 改动时间：2026-06-24。

## [14:40] 计划二次审计和提示词增强策略

- 用户要求：再次审计计划和二面题目，确认还需补充什么，并评估真实 LLM 下是否需要用户提示词增强机制。
- 改动内容：在 `plan/initial_solution.md` 补充真实 LLM 的用户提示词增强机制、增强安全边界、模型超时和增强异常 fallback、提交前交付清单，并将测试建议扩展到 17 条。
- 改动时间：2026-06-24。

## [14:50] 审批、安全配置和真实 LLM 策略确认

- 用户要求：明确 L2 审批后 `yes/allyes` 的精确行为，安全规则使用 JSON 配置，真实 LLM 超时 3 分钟、重试 3 次、流式输出、非法 JSON 重试一次再 fallback，ESC 单轮和会话都支持，README 到岗信息仅留空位。
- 改动内容：更新 `plan/initial_solution.md`，新增 `config/safety_rules.json` 和 `config/llm_config.json` 方案，明确 L2 审批后只允许 dry-run/诊断/解释/日志/安全建议，测试建议扩展到 21 条。
- 改动时间：2026-06-24。

## [15:00] 工作记忆策略确认

- 用户要求：同意采用“结构化工作记忆 + 最近 4 轮原文 + 历史摘要”的短期记忆方案，后续调整为更适合长上下文的最近 10 轮原文。
- 改动内容：更新 `plan/initial_solution.md`，新增 `config/memory_config.json`、默认 token 阈值、结构化压缩模板、上下文优先级和记忆冲突处理规则。
- 改动时间：2026-06-24。

## [15:10] 工作记忆阈值调整

- 用户要求：现代 LLM 下 1500 token 触发压缩过早，同意改为更适合 DeepSeek V4 Pro 长上下文的阈值。
- 改动内容：更新 `plan/initial_solution.md`，将 `memory_config.json` 默认值改为 `compress_trigger_tokens=48000`、`max_memory_tokens=64000`、`keep_last_turns=10`、`summary_max_tokens=4000`、`hard_trim_tokens=80000`。
- 改动时间：2026-06-24。

## [15:30] 运行日志和开发日志边界确认

- 用户要求：按照初始化 `AGENTS.md` 明确项目运行日志和开发日志不要冲突。
- 改动内容：更新 `plan/initial_solution.md` 和 `docs/index.md`，明确 `logs/`、`runs/` 是运行产物或运行证据，`docs/` 是开发日志和测试报告；正式测试结论必须写入 `docs/03-tests/`，不能只用运行 trace 代替。
- 改动时间：2026-06-24。
