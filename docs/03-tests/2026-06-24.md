---
tags: [tests, m1, m2]
date: 2026-06-24
summary: M1/M2 测试报告：19 passed，临时目录固化为项目内 .temp/pytest
status: done
---

# 测试报告

## 环境

- OS：Windows 11；Python 3.12.4；虚拟环境 `.venv`
- 依赖：pytest 9.1.1、pydantic 2.13.4、tiktoken 0.13.0、openai 2.43.0
- 配置目录：`config/`（默认）

## 稳定测试命令

```bash
.venv/Scripts/python.exe -m pytest tests/
```

- 临时目录已固化为项目内 `.temp/pytest`：在 `pyproject.toml` 中配置 `addopts = "--basetemp=.temp/pytest"`，避免系统 temp 目录权限问题导致测试不稳定。
- `.temp/` 已加入 `.gitignore`，不污染仓库。

## 结果摘要（2026-06-24）

| 模块 | 测试文件 | 用例数 | 结果 |
| --- | --- | --- | --- |
| M1 基础设施 | `tests/test_infra.py` | 11 | passed |
| M2 知识库 | `tests/test_knowledge.py` | 8 | passed |
| **合计** |  | **19** | **19 passed** |

### 关键验证点

- **M1**：AgentResponse 字段齐全可序列化；`confidence` 越界（-0.01/1.01/2.0）被 Pydantic 拒绝；配置正常加载、缺失/非法均回退保守默认并记 `config_warning`；trace 落盘含 trace_id 与 warnings；setup_logging 不重复挂载 handler。
- **M2**：E42 精确命中 `troubleshooting.md#e42`；最大速度+越界命中 `forbidden_actions`；抓取命中设备说明/安全规则；**无关查询返回空（不编造来源）**；编造的 source_id 被 `filter_sources` 过滤；全角转半角正确。

## 运行证据

- `runs/` 当前为空（CLI 尚未实现，单次 trace 待 M10 端到端运行产生）；本阶段测试证据为 pytest 控制台输出（19 passed）。
