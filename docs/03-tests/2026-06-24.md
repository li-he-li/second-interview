---
tags: [tests, m1, m2, m3, m4, m5, m6, m7, m8]
date: 2026-06-24
summary: M1-M8 测试报告：78 passed 0 warning，conftest 唯一临时目录 + 禁用 cache
status: done
---

# 测试报告

## 环境

- OS：Windows 11；Python 3.12.4；虚拟环境 `.venv`
- 依赖：pytest 9.1.1、pydantic 2.13.4、tiktoken 0.13.0、openai 2.43.0
- 配置目录：`config/`（默认）

## 稳定测试命令

```bash
.venv/Scripts/python.exe -m pytest tests/
```

- 临时目录由 `conftest.py` 的 `pytest_configure` 动态生成为项目内唯一子目录
  `.temp/pytest-<时间戳>-<pid>`，解决系统 temp 权限与固定目录复跑清理冲突。
- pytest cache 已禁用（`addopts = "-p no:cacheprovider"`），消除 `.pytest_cache` 权限 warning。
- 命令行显式 `--basetemp=xxx` 仍被尊重；`.temp/` 已在 `.gitignore`。

## 结果摘要（2026-06-24）

| 模块 | 测试文件 | 用例数 | 结果 |
| --- | --- | --- | --- |
| M1 基础设施 | `tests/test_infra.py` | 11 | passed |
| M2 知识库 | `tests/test_knowledge.py` | 8 | passed |
| M3 Mock 工具 | `tests/test_tools.py` | 11 | passed |
| M4 意图识别 | `tests/test_intent.py` | 9 | passed |
| M5 安全策略 | `tests/test_safety.py` | 16 | passed |
| M6 人工审批 | `tests/test_approval.py` | 7 | passed |
| M7 工作记忆 | `tests/test_memory.py` | 10 | passed |
| M8 ESC 打断 | `tests/test_interrupt.py` | 6 | passed |
| **合计** |  | **78** | **78 passed, 0 warning** |

### 关键验证点

- **M1**：模型字段校验、confidence 越界拒绝、配置保守回退、trace 落盘、日志去重。
- **M2**：E42 精确命中、越界命中 forbidden、无关查询返回空（不编造）、编造 source_id 被过滤。
- **M3**：三工具白名单、默认 dry-run、真实执行被拒、注入异常可控。
- **M4**：能力询问 vs 动作命令的意图边界、危险词→unsafe_action。
- **M5**：参数边界、设备拦截、L0/L1/L2 定级、**P0 回归：L2 审批不可被配置绕过**。
- **M6**：yes 单次 / no 拒绝 / allyes 全局放行、非交互默认拒绝、allyes 不降级。
- **M7**：压缩触发、保留最近轮次、pinned safety 不裁、硬裁剪兜底、**P1 回归：context_for_llm 按 max_memory_tokens 裁剪、summary 按 summary_max_tokens 裁剪保安全事实**。
- **M8**：cancel/check/reset、关键点检查抛 CancelledError（真实链路接入 + 集成测试列为 M9/M10 必做）。

## 运行证据

- `runs/` 当前为空（CLI 尚未实现，单次 trace 待 M10 端到端运行产生）；本阶段测试证据为 pytest 控制台输出（78 passed）。
